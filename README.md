# SprayCraft
SprayCraft is an A-CPS disease management system that models infection spread as spatial graphs. Message passing identifies disease hotspots and estimates infection intensity for variable-rate spraying, while the same graph is used to compute a near-optimal drone path via a TSP formulation for precision pesticide application.
